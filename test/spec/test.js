/*global describe, it */
'use strict';
(function () {
    describe('Git-Smack Chat App', function () {
    	it('should should have an array called messages', function () {

                    expect(messages !== undefined || null).to.equal(true);
                });




        // describe('unit test', function () {
    	// 	it('should confirm obj instance exists', function () {
     //            $('#submit-btn').click()
    	// 		expect(message).to.equal(true);
    	// 	})
    	// })

    
    })
})();
